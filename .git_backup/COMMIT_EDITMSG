Fix: Resolve syntax errors and clean up duplicate refresh code

- Fix try-catch-finally block structure in fetchProfile function
- Remove all duplicate refresh logic from refresh button
- Clean up orphaned catch blocks and syntax errors
- Centralize refresh functionality to use single fetchProfile function
- Add rate limiting to prevent 429 'Too many requests' error
- Remove unused state variables (isRetrying, retryCount)
- Fix all syntax errors and compilation issues
